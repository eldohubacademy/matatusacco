<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Trip</title>
    <link rel="stylesheet" href="main.css">
</head>
<body>
    <%- include("_header.ejs")%>
    <% if(locals.message){ %>
        <h1 style="color: orangered;"><%= message%></h1>
    <%}else{%>
        <main>
            <h1>New Trip</h1>

            <h2><%= numberPlate %></h2>

            <form action="http://localhost:3003/newtrip" method="post">
                <input type="hidden" name="numberPlate" value="<%= numberPlate %>" >
                <label for="route">Choose Route</label>
                <select name="route" id="route" required>
                    <% routes.forEach(r=>{%>
                        <option value="<%=r.route_id%>"><%=r.Label%>  </option>
                    <%})%>
                </select>
                <br>
                <label for="departure">Departure</label>
                <input type="datetime-local" name="departure" id="departure" required>
                <br>

                <input type="submit" value="Submit">
            </form>
        </main>
    <%}%>
</body>
</html>